from django.urls import include, path

from . import views

urlpatterns = [
    # path('', views.Applicant_ListView.as_view(), name='apllicant_changelist'),
    # path('add/', views.Applicant_CreateView.as_view(), name='apllicant_add'),
    # path('<int:pk>/', views.Applicant_UpdateView.as_view(), name='apllicant_change'),
    # path('ajax/load-leads/', views.load_leads, name='ajax_load_leads'),
]